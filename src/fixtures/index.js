const {
	input,
	expected,
	arrayInput,
	multiKeyArrayInput,
	expectedArray,
} = require('./deepRemoveKeyData');

module.exports = {
	input,
	expected,
	arrayInput,
	multiKeyArrayInput,
	expectedArray,
};
